const ROLES = require('./roles');
const { STATUS, OCCURRENCE_STATUS, NOTIFICATION_TYPES } = require('./statuses');

module.exports = {
    ROLES,
    STATUS,
    OCCURRENCE_STATUS,
    NOTIFICATION_TYPES
};